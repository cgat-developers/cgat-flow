
# This environment will include other dependencies that have not been detected automatically
# but are required to run the pipelines

name: cgat-f

channels:
- bioconda
- conda-forge
- defaults

dependencies:
- cutadapt=1.14
- blast=2.7.1
- bashlex=0.12
- pandoc=1.19.2
- mysqlclient=1.3.10
# conda env upgrade updates
- gmap=2017.05.08 # https://github.com/ContinuumIO/anaconda-issues/issues/704
